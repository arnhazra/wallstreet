name: Daily API Trigger

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call API
        run: |
          curl -X POST https://core-service-wallstreet.azurewebsites.net/apps/cashflow/execute
